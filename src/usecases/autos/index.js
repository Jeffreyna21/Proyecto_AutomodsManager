const buildListarAutos = require('./listarAutos');
const buildObtenerAuto = require('./obtenerAuto');
const buildCrearAuto = require('./crearAuto');
const buildActualizarAuto = require('./actualizarAuto');
const buildEliminarAuto = require('./eliminarAuto');

module.exports = {
  buildListarAutos,
  buildObtenerAuto,
  buildCrearAuto,
  buildActualizarAuto,
  buildEliminarAuto
};
